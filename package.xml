<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>drone_lib</name>
  <version>0.0.0</version>
  <description>TODO: Package description</description>
  <maintainer email="moisesmgft@gmail.com">Moisés Moreira Gonçalves Feltrin Thimoteo</maintainer>
  <license>TODO: License declaration</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- Fornece o FindEigen3.cmake usado pelo find_package(Eigen3) do
       CMakeLists. Estava sendo usado sem ser declarado: funcionava na
       maquina de quem ja tinha o pacote por outra via, e falhava numa
       instalacao limpa. -->
  <buildtool_depend>eigen3_cmake_module</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>px4_msgs</depend>
  <depend>tf2</depend>
  <!-- Chave do ROSDEP, nao o nome do pacote CMake. `Eigen3` (que estava
       aqui) nao existe no indice do rosdep; o `rosdep install` falhava com
       "Cannot locate rosdep definition for [Eigen3]". Funcionava so onde
       o Eigen ja tinha vindo por outra via. O find_package(Eigen3) do
       CMakeLists continua igual: sao espacos de nomes diferentes. -->
  <depend>eigen</depend>
  <depend>custom_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  
  <test_depend>ament_cmake_gtest</test_depend>
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
